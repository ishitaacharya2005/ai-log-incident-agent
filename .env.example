# Copy this file to .env and fill in your token to enable real LLM-based
# triage and report generation. Without it, the app runs in mock mode
# (deterministic, zero-cost, no API calls) so it's still fully demoable.

HUGGINGFACEHUB_API_TOKEN=your_hugging_face_token_here
